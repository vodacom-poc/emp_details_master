spring:
  application:
    name:  empdetailsmaster  



---
spring:
  profiles: default,native
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: leanarch-config-server