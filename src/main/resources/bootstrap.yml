spring:
  application:
    name:  empdetailsmaster  



---
spring:
  profiles: docker,native
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: leanarch-config-server